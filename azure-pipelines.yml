# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: Default

steps:
- task: wiz@1
  inputs:
    serviceConnectionId: 'wiz-devops-aluizio'
    command: 'scan'
    scanType: 'dir'
    path: '.'
    succeedOnFailures: false
    policies: 'vuln-one-medium'
